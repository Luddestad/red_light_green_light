# task0

A new Flutter project.

# Run

First try running it in the browser, to verify flutter is set up correctly:

`flutter run -d chrome`

Check available devices:

`flutter devices`

`flutter run -d <device-id>`
